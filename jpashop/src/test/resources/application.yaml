server:
  port: 8070

spring: #아래 설정이 없으면 기본으로 메모리 모드로 설정됨(test에서)
        ## p6spy 확인해보면 jdbc:h2:mem:~~ 로 돌아감
#  datasource:
#    url: jdbc:h2:mem:test #테스트용 메모리 모드 (DB 꺼져있어도 동작함)
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace